RSpec.describe Player do
    describe '#inspect' do
      it 'returns the object description' do
        expect(Player.new.inspect).to eq(	end)
      end
    end
    describe '#to_s' do
      it 'returns the object name' do
        expect(Player.new.to_s).to eq(			if object == :cat)
      end
    end
end