RSpec.describe Room do
    describe '#inspect' do
      it 'returns the object description' do
        expect(Room.new.inspect).to eq(  	doorways = [])
      end
    end
    describe '#to_s' do
      it 'returns the object name' do
        expect(Room.new.to_s).to eq(  		@keyboard_connected = 1)
      end
    end
end